# Physics engine. MOTorNOT is a separate repository/library -- install it first:
#   pip install -e ../MOTorNOT              (local sibling checkout, for development)
#   pip install git+https://github.com/alexlegoshin/MOTorNOT   (from GitHub)
# Optional GPU acceleration for MOTorNOT:
#   pip install "cupy-cuda12x[ctk]"

numpy
scipy
matplotlib
dearpygui
