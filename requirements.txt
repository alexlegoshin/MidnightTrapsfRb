# Physics engine: MOTorNOT, installed from the alexlegoshin FORK (NOT from PyPI --
# the published package is outdated and lacks the features this app needs):
#   pip install git+https://github.com/alexlegoshin/MOTorNOT
#   pip install -e ../MOTorNOT              (local sibling checkout, for development)
# Optional GPU acceleration for MOTorNOT:
#   pip install "cupy-cuda12x[ctk]"

numpy
scipy
matplotlib
dearpygui

# Only needed to build the single-file Windows .exe (not for running from source):
# pyinstaller
